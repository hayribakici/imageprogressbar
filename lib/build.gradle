apply plugin: 'com.android.library'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0"

    defaultConfig {
        minSdkVersion 11
        targetSdkVersion 25
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    dependencies { compile 'com.android.support:support-annotations:25.0.0' }
}
